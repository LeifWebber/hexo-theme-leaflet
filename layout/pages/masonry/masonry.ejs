<%
// Define an array of images to display in the masonry layout
const images = Array.isArray(theme.masonry) ? theme.masonry.filter(image => typeof image?.image === 'string' && image.image.trim()) : [];
%>

<h1 class="page-title-header">
	<%- getPageTitle(page) %>
</h1>

<div id="masonry-container">
	<% images.forEach(function(image) { %>
	<div class="masonry-item">
		<div class="image-container">
			<%- progressiveImage(image.image, { alt: image.title, kind: 'content', style: 'width:100%' }) %>
			<div class="image-title"><%- image.title %></div>
			<div class="image-description"><%- image.description %></div>
		</div>
	</div>
	<% }); %>
</div>
